This program takes in multiple numbers at the command line and prints the prime factorization for each of them.

The program should be called in the following way:
	java PrimeFact [n1] [n2] [n3] ...

The program creates an array of prime #s to use.
The # of primes in the array can be adjusted within the code by editing the # passed to init()